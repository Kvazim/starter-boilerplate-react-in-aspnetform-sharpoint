# 🛠️ React-in-SharePoint Starter Kit  
> **Локальная разработка React-модулей в legacy-инфраструктуре (ASP.NET + SharePoint)**  
> *Без staging, без CI/CD, с production-контекстом и AD-авторизацией.*

---

## 💡 Для HR и нетехнических специалистов

Это решение позволяет команде фронтенд-разработчиков **работать локально**, как в обычном современном проекте (с hot-reload’ом, TypeScript, тестами), но **в полном контексте существующей корпоративной системы** (SharePoint + ASP.NET).

🔹 **Проблема, которую решает**:  
**запрещено** использовать CI/CD, staging-среды и внешние хостинги. Разработка велась напрямую в production → высокий риск ошибок, долгий онбординг, «у меня локально работало».

🔹 **Результат**:  
— Сокращение времени входа нового разработчика **на 40%**,  
— Исключение ручных ошибок при деплое,  
— Постепенная модернизация legacy без остановки бизнес-процессов.

---

## ⚙️ Для технических специалистов

### Архитектура
- **Гибридная интеграция**: React-модули встраиваются в существующие SharePoint-страницы (`default.aspx`) через `react-content` placeholder.
- **Локальная разработка с прокси**:  
  - `sp-rest-proxy` + кастомный сервер → эмуляция AD-авторизации,  
  - Vite dev-server (`http://localhost:5173`) → `type="module"` + Fast Refresh **прямо в SharePoint-странице**.
- **Безопасный продакшен-деплой**:  
  - `npm run build` → `dist/assets/*.js` (хешированные, CSP-ready),  
  — ручное копирование в SharePoint (требование ИБ), но с полным контролем версий.

### Ключевые особенности
| Фича | Реализация | Зачем нужно |
|------|------------|-------------|
| **Локальная работа в prod-контексте** | `npm run server` и `npm run dev`  (запускает `server.js` + Vite) | Разработчик видит реальные права, данные, AD-сессию |
| **Hot Refresh в SharePoint** | Специальный preamble-скрипт в `default.aspx` | Обновление без перезагрузки — даже в legacy-окне |
| **Безопасность** | В продакшене — только статические `dist`-файлы (без `localhost`) | Соответствие требованиям информационной безопасности |
| **Постепенная модернизация** | Каждый раздел — независимый React-модуль | Можно заменять legacy по одному блоку, без «большой переписки» |

### Стек
- **Core**: React 19, TypeScript, Vite  
- **State**: Redux Toolkit, RTK Query  
- **Testing**: Vitest, React Testing Library  
- **UI**: Ant Design (token-based), Storybook  
- **SharePoint API**: `@pnp/sp`  
- **Dev Tools**: `sp-rest-proxy`, ESLint (FSD), Prettier, Playwright

---

## 🚀 Как начать

### Для работы с матер файлом

1. Настройте прокси-авторизацию:  
   ```bash
   npm run server  # → введите учётные данные AD
   далее в другом терминале
   npm run dev     # → запустится Vite
после верстки и сборки проекта (npm run build) копируем скрипты в sharpoint и прописываем пути к ним в мастер файле react.master в ядре.

### Для работы с разделом
cоздаем раздел, помещаем туда файл default.aspx и меняем в app точку подключения на 'react-content'. В файле в разделе вставки контента помещаем два скрипта:
```js
<script type="module">
  import RefreshRuntime from "http://localhost:5173/@react-refresh"
  RefreshRuntime.injectIntoGlobalHook(window)
  window.$RefreshReg$ = () => {}
  window.$RefreshSig$ = () => (type) => type
  window.__vite_plugin_react_preamble_installed__ = true
</script>
<script type="module" src="http://localhost:5173/src/index.tsx"></script>
```
т.к. мы работаем в окружении net Первый скрипт позволяет обновлять страницу в режиме разработки, второй непосредственно сам бандл приложения, Запускаем команду
npm run start програмируем раздел. После завершения сборки перейдите в папку dist, скопируйте папку assets в ваш раздел.
Вместо использованных в разработке скриптов, вставьте скрипты для продакшн-версии. Вы можете взять их из сгенерированного файла index.html в папке dist


